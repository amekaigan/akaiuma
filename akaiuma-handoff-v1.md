# akaiuma-handoff-v1.md

2026年9月11日時点の作業状況。
安定した仕様は `akaiuma-spec.md` を参照。このファイルには現在の状態、
未完了の作業、セッション中に判明したことのみを書く。

---

## 立ち上げの経緯

新規ジャンルでAI制作体制を実験する目的で始めた。当初は「小規模事業者向け
EC・決済ツール比較」を検討したが、以下の理由で「物販で稼ぎたい人向けの
実務ノウハウメディア」に変更した。

- 一般的な「稼ぐ」ジャンルは運営者の20年の専門性が活きない
- 物販領域なら「稼ぎたい人」の需要と運営者の専門性が完全に重なる
- 自社の在庫・需要予測ツールが、そのまま読者の課題への解になる

サイト基盤は asiatopro.com の `generate-site.js` を土台に、テーマ依存部分を
書き換えて構築した。

---

## 完了している作業

### インフラ

- [x] ドメイン取得（akaiuma.com、お名前.com）
- [x] DNS設定（Aレコード4本 + www CNAME）
- [x] GitHubリポジトリ作成（amekaigan/akaiuma）
- [x] GitHub Pages 有効化（main / root、CNAME設置済み）

### ファイル

- [x] `CLAUDE.md`
- [x] `.claude/agents/` 5体（researcher / writer / seo / proofreader / sns）
- [x] `.github/scripts/generate-site.js`
- [x] `assets/css/article.css`
- [x] `assets/logo/` ロゴ一式
- [x] `index.html`（トップ）
- [x] `blog/index.html`（記事一覧）
- [x] `blog/_template/index.html`（記事テンプレート）
- [x] `about/index.html`
- [x] `knowledge/inbox.md`（`index.md` と `inventory.md` は未作成）
- [x] 記事1本目 `blog/zaiko-awanai-riyu/index.html`

### ロゴ

角のある赤い馬の頭部シルエット。`#C8102E` 単色。
32pxまで縮小しても形が残るよう、面の数を減らして角を立てた。

- `logo.svg` 横組みロックアップ
- `logo-mark.svg` マーク単体（赤）
- `logo-white.svg` 白抜き（ヘッダー・フッター用）
- `logo-mono.svg` `currentColor` 対応
- `favicon.svg` / `favicon-32.png` / `apple-touch-icon.png` / `og-default.png`

マスコットは別扱い。ChatGPTで生成した疾走する馬のイラストを、SNSアイコンと
サムネイル専用に使う。**サイトのロゴとしては使わない。**中級層向けの記事の
信頼性と噛み合わないため。

### ショートカット（素材投稿）

iOSショートカット「あかいうま」から `knowledge/inbox.md` に追記する仕組みを構築済み。
ホーム画面にアイコン設置済み。構成の詳細は `akaiuma-spec.md` の8章を参照。

- Fine-grained token（akaiuma リポジトリの Contents: Read and write のみ）
- 有効期限1年。期限切れ時は再発行してショートカット内の「テキスト」アクションを差し替える
- 見出し形式 `## (YYYY-MM-DD HH:mm)` で区切る

**詰まった点：** GitHub API は `User-Agent` ヘッダーがないと接続を拒否する。
これがないと「ネットワーク接続が切れました」と表示され、ネットワークの問題と
誤認しやすい。この解決策は Gemini の指摘によるもの。

**ショートカットアプリの操作で詰まった点：**

- 入力欄に入ってしまった変数は、タップ → 変数の詳細パネル → 「変数を消去」でしか消せない
- URL欄へのペーストが効かないことがある。効かない場合は手打ちするしかない
- アクション名が紛らわしい。「URL」（用意するだけ）と「URLの内容を取得」（通信する）は別物

---

## 未完了の作業

### 優先度：高

- [ ] **DNS反映待ち。** 設定は完了しているが、9月11日夜時点で未反映。
      GitHub Pages の設定画面は「DNS Check in Progress」
- [ ] **HTTPS有効化。** DNS反映後、「Enforce HTTPS」にチェックを入れる。
      証明書発行まで時間がかかるため、押せない場合は30分ほど待つ
- [ ] **GA4の設置。** `generate-site.js` の `GA4_ID` が空。
      測定ID取得後に記入すれば、level と category がカスタムパラメータとして送られる
- [ ] **GitHub Actions のワークフロー未作成。** asiatopro のものを流用する。
      SNS投稿の順序バグ（コミット前に投稿してしまう問題）は修正済みの版を使う

### 優先度：中

- [ ] フッターのXアカウントURLが空（`https://x.com/` のまま）
- [ ] `/tool/` ページが未作成。ヘッダーとフッターからリンクしているが404
- [ ] サムネイル生成スクリプト（`gen-thumbs.js`）未移植
- [ ] ASPの申し込み。物販系の案件はA8・afb・もしも・レントラックスあたり
- [ ] プライバシーポリシー（GA4導入時に必要）

### 優先度：低

- [ ] 問い合わせ先は当面 `info@asiatopro.com` を使う方針。
      Cloudflare Email Routing への移行はツール販売開始時にまとめて行う
- [ ] 記事2本目以降。周辺記事をエージェントに書かせて実力を測る

---

## 記事1本目について

`/blog/zaiko-awanai-riyu/`
「年間1万件出荷して分かった、在庫が合わない本当の理由」
level: intermediate / category: 在庫管理

運営者の実体験をもとに、私（Claude）が執筆した。エージェントには投げていない。
2本目以降の見本として使う想定。

### 記事の主張

在庫が合わない原因は Excel ではなく、**在庫の「定義」と「共有」が
決まっていないこと**。問題は2階建て。

1階：数字がずれる。移動中・引き当て済み・梱包済み・FBA返品処理中の
4地点でルールがないため。毎月半日をずれの修正に使っていた。

2階：数字が合っていても判断できない。発注状況が担当者の頭の中にあるため、
FBAへ何個送るべきか決められない。置き場所も記憶に依存すると、
棚卸しもピッキングも止まる。

前兆は「Aさんに聞かないと分からない」という言葉が出始めたとき。

**確認事項：** 「毎月半日」という数字と年間6日換算を記載している。
公開して問題ないか未確認。差し支えあれば該当段落を削除できる。

---

## 判明した技術的な注意点

### generate-site.js

- `_` で始まるディレクトリは記事として扱わない（`_template` 対策）
- `disclosure` フィールドは `extractMeta` に追加済み
- 色は `C` オブジェクトに集約。個別のリテラルを直さないこと
- asiatopro の `re-` プレフィックスは `ak-` に変更している。
  asiatopro のCSSを流用する際はクラス名の置換が必要

### ナビゲーションの幅

スマホ表示でメニューが画面幅に収まらない問題があり、`width:100%` を
`width:100vw; max-width:100vw; flex:0 0 auto;` に変更した。
asiatopro で先に発見された問題。

### テーブル

asiatopro と同じラッパー方式に統一した（`ak-table-wrap` + `ak-table-hint`）。
`min-width: 560px` でスクロールを発生させ、767px以下でヒントを表示する。

---

## 次のセッションで最初にやること

1. `akaiuma.com` が表示されるか確認
2. 表示を見てCSSの調整（実物を見ないと判断できない箇所がある）
3. GA4の設置
4. GitHub Actions のワークフロー作成

### 開始時の投げ方

```
akaiuma.com の続き。リポジトリは amekaigan/akaiuma。
akaiuma-spec.md と akaiuma-handoff-v1.md を読んでから始めてください。
```

---

## セッション中に決めたこと（記録）

- 名前は `terastbase.com` も候補だったが、4文字カタカナ造語は記憶の中で
  他社名と混ざるため見送り。三国志の赤兎馬が由来の `akaiuma.com` を採用
- 屋号はひらがな表記。警戒心を下げる効果とマスコットとの相性を優先
- 「扱わないこと」「読者への約束」は about に書かない。自分を縛るため。
  代わりに「どんな人に向けて書いているか」「記事を書くときに考えていること」とした
- CLAUDE.md の断定禁止ルールは、エージェント限定に変更。
  運営者本人の経験にもとづく記述には適用しない
