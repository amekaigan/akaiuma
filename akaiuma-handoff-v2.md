# akaiuma-handoff-v2.md

2026年9月18日時点の作業状況。`akaiuma-handoff-v1.md`（9月11日）からの差分を書く。
安定した仕様は `akaiuma-spec.md`、運用ルールの正本は `CLAUDE.md`。

**このファイルは asiatopro との共有を前提に、隠さず書いている。**
ただしリポジトリが public のため、契約条件（各モールで実際に適用されている料率など）は
意図的に書いていない。数値が必要な場合は運営者に直接確認すること。

---

## 1. この1週間で何が変わったか

9月11日時点：記事2本、サイト基盤とエージェント5体。
9月18日時点：記事3本、エージェント6体、装飾・画像・自動化の仕組みが入った。

| 領域 | 状態 |
|---|---|
| 公開記事 | 3本（初心者2／中級1。在庫管理2／販路・集客1） |
| エージェント | 6体（researcher / writer / **editor** / seo / proofreader / sns） |
| 記事の見た目 | 装飾パーツ8種、SVG図解、自動生成バナー、読了時間、進捗バー |
| 自動化 | Routine 2本（週次の記事生産／日次の質問）＋ 手順書2本 |
| 素材の蓄積 | 体験バンク、企画リスト、質問キュー、research 3ファイル |

---

## 2. 新設したもの

### 2-1. エディター（読みやすさ・リズム担当）

`.claude/agents/editor.md`

**背景**：記事は内容が正しくても「よく読めば分かる」状態で止まっていた。回りくどく、
文字だけが続き、教科書のような見た目だった。読みやすさ専門の役職を分離した。

**基準（CLAUDE.md 4節「読みやすさとリズム」にも記載）**

- 一文60文字目安、80文字超は分割
- 1段落3〜4文。1段落で言うことは1つ
- 長短の文を交ぜる。同じ文末を3連続させない
- 冗長表現の置換表（`〜することができます`→`できます` 等）
- 見出しをまたぐ箇所に次への引きを1行（空の接続は禁止）
- 漢字をひらく基準

**触らせないもの**：事実・数字・確度の書き分け・運営者の実体験・TODO コメント・メタ情報。
「短くするのは言い回しであって、情報ではない」と明記している。

### 2-2. 記事内の装飾パーツ（`assets/css/article.css`）

記事側に `style` 属性を書かせないための共通クラス。

| クラス | 用途 |
|---|---|
| `p.ak-lead` | h2直後の一行要約（罫なし・下線のみ） |
| `div.ak-point` | 要点・結論のまとめ |
| `p.ak-pull` | 核心の一文を抜き出す |
| `ol.ak-steps` | 順番に意味がある手順 |
| `ul.ak-check` | 今日やることのチェックリスト |
| `div.ak-compare` | 悪い例と良い例の対比 |
| `hr.ak-sep` | 場面転換（h2直前には置かない） |
| `figure.ak-figure` | SVG図解 |

**h2ひとつにつきパーツは1つまで。**ただし `ak-note`（実体験）と `ak-figure`（図解）は
装飾ではなく中身なので、この数に含めない。

### 2-3. 画像まわり（`generate-site.js`）

**サムネイルの自動生成**：記事に画像がなければ、バナーをSVGで生成する
（`assets/thumb/auto/<slug>.svg`）。骨格は全記事共通で、変わるのは3つだけ。

- カテゴリごとの地色とアイコン（`CATEGORY_LOOK`。虫眼鏡・箱・カート・棚・棒グラフ・歯車）
- メタ情報 `thumbcopy` の短いコピー（18字以内）
- 背景にロゴマークの透かし（不透明度5.5%、カード右下で切る）

優先順位は `thumb:` ＞ `assets/thumb/<slug>.(svg|png|webp|jpg)` ＞ 自動生成。

**記事内の図解**：`assets/figure/` にSVGを置く。**記事の中身を図にしたものだけ**で、
雰囲気だけの写真は使わない（素材写真はどの記事でも成立してしまい、一次情報の価値を薄める）。

**注意**：SVGはSNSのOGP画像として表示されない。`og:image` はPNGのまま。

### 2-4. 記事を開いた読者に、読む理由を渡す

記事冒頭に自動で入る4点。書き手が用意するのは `takeaways` だけ。

| 要素 | 出どころ |
|---|---|
| サムネイルのバナー | 自動 |
| 読了時間（約◯分） | 自動（本文の文字数 ÷ 600字） |
| 読み進みのバー | 自動（画面上部） |
| この記事で分かること | メタ情報 `takeaways`（`|` 区切りで3点） |

### 2-5. メタ情報の追加フィールド

```
thumbcopy:      サムネイルのコピー。18字以内
takeaways:      この記事で分かること。| 区切りで3点
search_intent:  検索者の問いを1文で
title_reason:   なぜこのタイトルがその問いへの答えなのか
```

`search_intent` / `title_reason` は **asiatopro から取り込んだルール**。
ページには出力せず、切り口の検証用に残す。生成スクリプトは未知のキーを無視するので、
フィールド追加でスクリプトを触る必要はない。

---

## 3. 追加したルール（CLAUDE.md）

### 3-1. 数値が確認できないときは、空欄で確保する

```
<!-- TODO:数値 何を入れるか：（項目名）／出典：（公式URL）／確認日：（入れた日） -->
```

検索結果の要約から拾った数値を、確認済みのように書かない。空欄が残る記事は公開しない。
**空欄のまま論旨が成立するなら、その記事は数値がなくても価値がある**とも書いた。

### 3-2. 記事の切り口を決める手順（asiatopro から取り込み）

執筆より前に通す5ステップ。検索者の問いを1文で書く／タイトルをその答えにする／
差別化は書き方で出す／調査中の発見はタイトルの軸にしない／運営者の仮説を前提にする。

### 3-3. 事実確認（asiatopro から取り込み）

- 「〜は誤り」と断定する前に、一次資料の本文を開く
- 書き始める前に既存記事を grep し、同じ数値や論点の重複を避ける

---

## 4. 自動化（9月18日に稼働）

**背景**：運営者が動かないとサイトの更新が止まる。5日空いた。
工程を分解すると、止まる原因は**実体験の注入1箇所**だけだった。
そこ以外を自動化し、実体験は「先に貯める」形にした。

### Routine 2本（Claude Code の Routines として稼働中）

| 名前 | 時刻 | 内容 |
|---|---|---|
| あかいうま｜週次の記事生産 | 毎週月曜 8:00 JST | 企画を1本取り、調査→執筆→推敲→校正。下書きをブランチに置く |
| あかいうま｜1日1問の体験バンク | 毎日 20:00 JST | 実体験の質問を1つだけ送り、答えを体験バンクに貯める |

### 手順書（`.claude/workflows/`）

- `週次の記事生産.md`
- `日次の質問.md`

### 蓄積ファイル（`knowledge/`）

- `記事の企画リスト.md` — 次に書く記事の待ち行列（状態：未着手／下書き中／回答待ち／公開済み）
- `体験バンク.md` — 運営者の実体験を、記事を書く前に貯める
- `質問キュー.md` — 1日1問ずつ聞く問いの待ち行列
- `inbox.md` — 思いつきの放り込み先（v1から存在）

### 自動セッションに禁じたこと

1. `TODO:実体験` を自分で埋めない
2. main にマージしない（下書きはブランチ止まり）
3. 公式ページで確認できない数値を書かない

**ここを緩めると、自動化した瞬間に「誰でも書ける記事」に落ちる。**
不便なまま残しているのは意図的。

---

## 5. 環境の制約（重要・asiatopro でも同じ可能性がある）

### 5-1. 公式ドメインへの接続が遮断されている

Claude Code on the web のネットワークポリシーにより、以下への接続が
ゲートウェイで拒否される（403）。

```
www.rakuten.co.jp / corp.rakuten.co.jp
sell.amazon.co.jp / services.amazon.co.jp
www.shopify.com / thebase.com / www.lycorp.co.jp
```

**結果**：リサーチ担当は検索エンジンの要約までしか取れず、一次資料の原文照合ができない。
手数料・規約を扱う記事では、**運営者が公式ページのスクリーンショットを貼る**運用にした。
実際、3モールぶんの料金はこの方法で確認・記録した。

### 5-2. PDFが読めない

`pdftoppm`（poppler-utils）も `pypdf` も入っておらず、プロキシ経由の `pip install` も
タイムアウトする。**PDFを渡されても読めない。スクリーンショットなら読める。**

### 5-3. 使えるもの

- Chromium（`/opt/pw-browsers/`）でのレンダリング確認・スクリーンショット撮影
- ローカルHTTPサーバーを立てて、CSS込みで実機に近い表示検証
- 上記を使い、サムネイルSVGの見た目、進捗バーの動作、横スクロールの有無を実測した

---

## 6. この1週間で踏んだ失敗（同じ轍を踏まないために）

| 失敗 | 原因 | 対策として入れたもの |
|---|---|---|
| 手数料に「配送料を含む」と断定 | 運営者の説明に、こちらの解釈を足した | CLAUDE.md「運営者の仮説を前提にする」 |
| 自店の優遇料率を記事に載せた | 一般化できない数字を一般の数字として書いた | 「契約条件は記事にもメモにも書かない」 |
| タイトルが一覧で途中までしか出ない | `｜` が2つあると、生成スクリプトが副題として切る | タイトル内の区切りは `：` を使う |
| 画像が画面からはみ出す | 狭い画面で `width:auto` にし、img本来の1200pxに戻った | `width: min(720px, calc(100% - 32px))` |
| 進捗バーが動かない | スクリプトが本文より前に置かれ、要素を掴めていなかった | `DOMContentLoaded` を待つ |
| 未完成の記事を main にコミット | ブランチ確認を怠った | 下書きは必ずブランチ。コミット前に `git branch --show-current` |
| `ak-sep`（•••）が意味不明 | h2の直前に置き、見出しと役割が重複した | 「h2の直前に置かない」をルール化 |

**共通点：どれも「確認すれば防げた」もの。**特に上2つは、運営者の言葉に
こちらの解釈を足したことが原因で、記事の信頼性に直結する。

---

## 7. 公開中の記事3本

| slug | タイトル | level | カテゴリ |
|---|---|---|---|
| `zaiko-awanai-riyu` | 年間1万件出荷して分かった、在庫が合わない本当の理由 | intermediate | 在庫管理 |
| `zaiko-mochisugi` | 在庫を持ちすぎるのに欠品する理由 | beginner | 在庫管理 |
| `mall-erabikata` | 販路の選び方：楽天・Amazon・自社サイトどれが先 | beginner | 販路・集客 |

3本目の作り方が、この体制の見本になっている。

1. SEO担当が切り口とキーワードを設計
2. リサーチ担当が公式情報を収集（→ 接続遮断で失敗。数値は空欄で確保）
3. 記事作成担当が、数値なしでも論旨が立つ構成で草稿
4. **運営者が実体験を注入**（3チャネルを一つずつ始める理由、配送ラベル、自社サイトへの移行順序、商品が売れる循環）
5. エディターが文体統一と装飾
6. 校正担当が確度と表記を点検 → 指摘6件をすべて反映
7. 運営者が公式ページのスクショで数値を確認 → 反映して公開

**所要：半日。運営者の実作業は、質問への回答とスクショ提供のみ。**

---

## 8. 未完了の作業

- [ ] 既存2記事から3本目への内部リンクの張り直し（SEO担当の設計にある2本）
- [ ] `akaiuma-spec.md` の更新（9月11日のまま。本ファイルの内容を反映させる必要がある）
- [ ] OGP画像のPNG化（SVGはSNSで表示されない。`sharp` 等の導入が必要）
- [ ] `/tool/` ページ（値付けシートの配布構想。有料でなくても、作るならディレクター確認が要る）
- [ ] 企画リストの4本（FBA／限界利益と上限広告費／売れない商品の直し方／在庫の定義）

---

## 9. asiatopro に流用できそうなもの

コードとして持っていけるもの：

- `assets/css/article.css` の装飾パーツ（クラス名を変えれば、そのまま使える）
- `generate-site.js` のサムネイル自動生成（`buildAutoThumb` / `CATEGORY_LOOK` / `iconPath` / `wrapForThumb`）
- 記事冒頭の4点（読了時間の算出、進捗バー、takeaways ブロック）

運用として持っていけるもの：

- **エディターという役職の分離**（書く人と読みやすくする人を分ける）
- **`TODO:実体験` と `TODO:数値` の空欄確保**（埋められないものを埋めない仕組み）
- **体験バンク**（記事を書くときに聞くのではなく、先に貯める）
- **1日1問**（人間の負担を1〜2分に圧縮する）
- 工程ごとにセッションを切る運用（会話履歴の蓄積が使用量の主因）

逆に asiatopro から取り込んだのは、切り口を決める5ステップと事実確認の2点。
**この往復はうまくいっている。**

---

## 10. 共有にあたっての注意

- **リポジトリは public。** `research/` も `knowledge/` も誰でも読める。
  契約条件・仕入先・取引先の情報は書かない
- **git履歴にも注意。** 9月15日のコミット（`6ea7849` `d26df78`）には、
  現在は削除済みの契約条件に関する記述が残っている。履歴から完全に消すには
  履歴の書き換えと force-push が必要で、GitHub のキャッシュやフォークには
  残る可能性がある。対応するかは運営者の判断
- 記事本文・CLAUDE.md・エージェント定義・手順書には、公開して困る情報は入っていない
