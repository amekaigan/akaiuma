# akaiuma-spec.md

あかいうま（akaiuma.com）の仕様書。
方針・アーキテクチャ・運用ルールを記載する。安定した内容のみを置き、
現在の作業状況や未解決の課題は `akaiuma-handoff-vN.md` に書く。

最終更新：2026-09-11

---

## 1. サイトの基本情報

```
ドメイン：akaiuma.com
屋号：あかいうま
タグライン：物販の売上アップと業務効率化
GitHub：amekaigan/akaiuma
ホスティング：GitHub Pages（main / root）
由来：赤兎馬 — 乗り手の力を引き出し、千里を走る馬
```

### 表記ルール（ぶらしてはならない）

| 場面 | 表記 |
|---|---|
| ロゴ・サイトタイトル・記事内の基本 | あかいうま |
| SNS表示名 | あかいうま｜物販の売上アップと業務効率化 |
| 文中で強調する場合のみ | 赤い馬 |
| 由来を語る場面のみ | 赤兎馬 |

読みは常に「あかいうま」。

### ブランドの構造

赤兎馬は乗り手を選ばず、乗った者の力を引き出した馬である。
**読者が乗り手であり、サイトは馬。主役は常に読者であって運営者ではない。**
「私がすごい」ではなく「あなたが速く走れる」という書き方を全記事で貫く。

---

## 2. 事業設計

### 収益の三層

| 層 | 内容 | 時期 | 位置づけ |
|---|---|---|---|
| 一次 | ASP経由のアフィリエイト報酬 | 3〜6ヶ月 | 初期キャッシュ |
| 二次 | 自社の在庫・需要予測ツールの月額課金 | 12ヶ月〜 | 本命。ストック型 |
| 三次 | 有料コンテンツ・個別支援 | 未定 | 需要が見えてから判断 |

**このメディアは「稼ぐ場所」ではなく「信頼を作り、ツールへ送る場所」。**
短期の報酬のために記事の正確性を曲げない。

### 読者像

物販で稼ぎたい、または稼ぎ始めて伸び悩んでいる個人・小規模事業者。

| 層 | 状態 | 記事での役割 |
|---|---|---|
| 初心者 | 月商0〜30万。何を売るかで迷っている | 検索流入の入口。母数が最大 |
| 中級 | 月商50〜500万。管理が破綻し始めている | 収益の主戦場。競合が最も薄い |

開始時点では層を決め打ちしない。両方に記事を出し、3ヶ月後に数字で判断する。

### 3ヶ月後の判断基準

`level` 別に以下を集計する。

- 滞在時間・読了率
- メール登録率
- アフィリエイトのクリック率と成約率
- SNSでの反応の質

**PV数だけで判断してはならない。**初心者向けは母数の差で必ずPVが多く出る。
これを根拠に寄せると、単価の低い層に最適化されてしまう。

### 差別化の源泉

運営者が現在も現場で手を動かしている点。EC・マーケティング歴20年、
年商1億円規模のECショップ店長。複数チャネル（楽天・Yahoo!・Amazon・自社サイト）の
実運用経験がある。

**AIは取材・構成・整形・校正を高速化する道具であり、専門性そのものを生成できない。**

---

## 3. サイト構造

```
akaiuma/
├── index.html              トップページ
├── CNAME                   akaiuma.com
├── favicon.svg / favicon-32.png / apple-touch-icon.png / og-default.png
├── CLAUDE.md               エージェント向けの全ルール
├── .claude/agents/         サブエージェント5体
├── .github/scripts/
│   └── generate-site.js    サイト生成スクリプト
├── assets/
│   ├── css/article.css
│   └── logo/               ロゴ一式
├── blog/
│   ├── index.html          記事一覧
│   ├── _template/          記事テンプレート（記事として扱われない）
│   └── <slug>/index.html   個別記事
├── about/index.html        このサイトについて
└── knowledge/              運営者の一次情報（後述）
```

記事URLは `/blog/<slug>/`。

### カテゴリ

```
リサーチ | 仕入れ | 販路・集客 | 在庫管理 | 数字管理 | 業務効率化
```

カテゴリIDは順に `research` `sourcing` `sales` `inventory` `numbers` `efficiency`。
URL構造に影響するため、変更時はリダイレクトの要否を判断すること。

---

## 4. 記事の仕様

### メタ情報（HTMLコメントで冒頭に記述）

```html
<!--
title: 記事タイトル｜あかいうま
meta: 説明文（70文字以内）
permalink: /blog/slug/
published: YYYY-MM-DD
updated:
verified: YYYY-MM-DD
category: 在庫管理
level: beginner | intermediate
disclosure: affiliate | owned | both
thumb:
-->
```

- `level` は必須。省略するとビルド時に警告が出る
- `verified` は料金・仕様を確認した日。記事上部に表示される
- `disclosure` を入れると開示ブロックが自動挿入される

### AUTOマーカー

`generate-site.js` が中身を自動生成する箇所。

| マーカー | 内容 |
|---|---|
| `HEAD` | CSS読み込み、GA4タグ（`</head>` 直前に自動挿入） |
| `HEADER` | ヘッダー、お知らせバー、構造化データ |
| `ARTICLEHEAD` | 記事タイトル・カテゴリ・日付・開示ブロック |
| `TOC` | 目次（h2が4つ以上の記事に自動挿入） |
| `AD` | PR枠（h2が5つ以上の記事の3つ目のh2直前） |
| `BELOW` | 中級導線・CTA・関連記事・カテゴリ・最新記事・フッター |
| `SLIDER` | トップページのおすすめ記事 |
| `latest` | トップページの新着記事 |
| `research` 他 | 記事一覧のカテゴリ別ブロック |

### 本文で使うクラス

| クラス | 用途 |
|---|---|
| `ak-article` | 記事本文の囲み。これがないと目次が生成されない |
| `ak-note` | 実体験の注入ブロック（赤枠） |
| `ak-source` | 出典と確認日の小さい文字 |
| `ak-table-wrap` | テーブルの囲み。横スクロール案内が自動挿入される |
| `num` | 数値セルの右揃え |

### テーブルの書き方

```html
<div class="ak-table-wrap">
<table>
  <tr><th>項目</th><th>数値</th></tr>
  <tr><td>出荷件数</td><td class="num">10,000</td></tr>
</table>
</div>
```

「← 横にスクロールできます →」の案内はビルド時に自動挿入される（767px以下で表示）。

---

## 5. 編集方針

### 守ること

1. **エージェントは一次体験のない断定を書かない**
   運営者本人が加筆する部分には適用しない。20年の経験にもとづく判断は、
   データで証明できなくても価値がある。AIがそれを真似てはならない、という趣旨。

2. **一般論で埋めない**
   「自分に合った方法を選びましょう」のような、誰でも書ける文章は削除対象。
   判定基準：実務経験のない書き手にも書けるか。

3. **出典を持つ**
   手数料・規約・モール仕様は公式情報を出典とする。
   まとめサイトや他人の記事を出典にしない。

4. **情報の鮮度を記録する**
   手数料率・料金・仕様には `verified` を添える。

5. **収益を保証しない**
   「必ず稼げる」「誰でも月◯万」を禁止する。

6. **確度を書き分ける**
   公式情報にもとづく事実、実際に試したこと、経験からの推測。
   **どれを書いてもよいが、どれなのかが読者に分かるようにする。**
   禁止するのは断定そのものではなく、確度の異なるものが混ざることである。

### 実体験の注入

エージェントは運営者の経験が必要な箇所を空欄として残す。

```html
<!-- TODO:実体験 何を書くべきか：（運営者がそのまま答えられる具体的な問い） -->
```

問いは具体的に書く。「体験を入れる」ではなく
「複数チャネルの在庫を一元化したとき、最初に破綻したのはどの工程か」のように。

### 開示義務

- アフィリエイトリンクを含む記事はPR表記
- 自社ツールに言及する記事は運営者が開発・販売者である旨を明示
- 「第三者の中立的な比較」を装う表現は使わない
- 有料販売を始める場合、特定商取引法の表示が必要。法人名義で行う

---

## 6. SEO方針

- 狙うキーワードは記事1本につき主1・副3まで
- タイトルは32文字以内。主キーワードは先頭30文字以内（スマホ表示の制限）
- メタディスクリプションは70文字以内。具体的な名詞と数字を入れる
- アンカーテキストは内容を示すもの。「こちらの記事」は禁止
- 内部リンクは公開時に3本以上
- 公開ペースは週4〜5本を上限とし、余力は既存記事の改善に回す

**量産で勝とうとしない。**物販・副業系は専業アフィリエイターと企業メディアが
長年張り付いている激戦区。記事数では勝てない。勝てるのは一次情報の深さだけ。

---

## 7. エージェント体制

```
ディレクター（親セッション・Opus）
  ├─ researcher    リサーチ担当（Sonnet）→ research/
  ├─ writer        記事作成担当（Sonnet）→ blog/
  ├─ seo           SEO担当（Sonnet）→ keywords/
  ├─ proofreader   校正担当（Sonnet）
  └─ sns           SNS担当（Haiku）→ sns/
```

サブエージェント同士は直接会話できない。報告は必ずディレクターに戻る。
ハブ型で運用する。

**1セッションに全工程を詰め込まない。工程ごとにセッションを切る。**
会話履歴の蓄積が使用量消費の主因であり、これが最も効く節約になる。

### 記事の種類と分担

| 種類 | 頻度 | 担当 |
|---|---|---|
| 主力記事 | 月2〜4本 | 運営者の経験が核。knowledge/ を素材に作る |
| 周辺記事 | 週3〜4本 | 手数料比較・用語解説など。エージェントが公式情報から書ける |

周辺記事が検索の入口になり、主力記事へ内部リンクで送る。

---

## 8. knowledge/ — 運営者の一次情報

`research/`（エージェントが調べた結果）とは別物。混ぜない。

### タグ

| タグ | 意味 | 記事での扱い |
|---|---|---|
| `[実測]` | 自分でやって確かめた。数字や日付がある | 断定してよい |
| `[経験]` | 何度も経験して身についた判断 | 「経験上」として書く |
| `[推測]` | そう感じるが確証はない | 「〜と考えている」として書く |
| `[公式]` | 公式情報。確認日を添える | 出典付きで断定 |

### inbox.md への投稿

iOSショートカット「あかいうま」から GitHub API 経由で追記する。

出力形式：

```
## (YYYY-MM-DD HH:mm)

（本文、何行でも）
```

思いついたときに放り込む運用。整理・分類はしない。
タグ付けと分類は後から行う。

### ショートカットの構成

```
1.  「素材を入力」でテキストを要求
2.  現在の日付
3.  日付を書式設定（カスタム：yyyy-MM-dd HH:mm）
4.  テキスト（トークンを保持。GETとPUTの両方から参照）
5.  URLの内容を取得【GET】
6.  URLの内容 内の sha の値を取得
7.  URLの内容 内の content の値を取得
8.  辞書の値を base64 でデコード
9.  テキスト（デコード結果 + 改行 + ## (日付) + 入力内容）
10. テキストを base64 でエンコード
11. URLの内容を取得【PUT】
```

エンドポイント（GET / PUT 共通）：

```
https://api.github.com/repos/amekaigan/akaiuma/contents/knowledge/inbox.md
```

必須ヘッダー：

| キー | 値 |
|---|---|
| `Authorization` | `Bearer ` + トークン |
| `Accept` | `application/vnd.github+json` |
| `User-Agent` | `ShortcutsApp`（任意の文字列。**ないと接続を拒否される**） |
| `Cache-Control` | `no-cache`（連続投稿で古い内容を掴まないため。GETのみ） |

PUT の本文（JSON）：

| フィールド | 値 |
|---|---|
| `message` | `Update inbox.md via iOS Shortcut` |
| `content` | Base64エンコードした全文 |
| `sha` | GETで取得した sha |

### 認証

Fine-grained token。`akaiuma` リポジトリの **Contents: Read and write** のみ。
有効期限1年。期限切れ時は再発行し、ショートカット内の「テキスト」アクション
（トークンを保持している箇所）を差し替える。**1箇所直せば両方に反映される。**

### 詰まりやすい点

- GitHub API は `User-Agent` がないとリクエストを拒否する。
  ショートカットでは「ネットワーク接続が切れました」と表示されるため、
  ネットワークの問題と誤認しやすい
- GitHub API に追記のエンドポイントはない。
  取得 → デコード → 連結 → エンコード → 全文を書き戻す、という手順になる
- `sha` を送らないと更新が拒否される

---

## 9. 公開前チェックリスト

- [ ] `level` と `category` がフロントマターにあるか
- [ ] `verified` が入っているか（料金・仕様に言及する場合）
- [ ] PR表記があるか（アフィリエイトリンクを含む場合）
- [ ] 自社ツールへの言及がある場合、運営者との関係を開示しているか
- [ ] `TODO:実体験` がすべて埋まっているか
- [ ] エージェントが書いた部分に、一次体験のない断定が残っていないか
- [ ] 収益を保証する表現がないか
- [ ] 出典が公式情報になっているか
- [ ] 初心者記事の場合、中級記事への導線があるか（自動挿入されるが確認）
- [ ] 内部リンクが3本以上あるか
- [ ] タイトルが32文字以内で、主キーワードが先頭30文字以内にあるか

---

## 10. 作業の進め方

### コードの受け渡し

- 1〜4箇所の変更：**「検索」「置き換え」形式**で正確な文字列を提示
- 構造的な書き換え、5箇所以上に散る変更：ファイル全体を差し替え

この形式は固定。

### 注意点

- GitHubのWeb editorで「すべて置換」を使わない。意図しない箇所まで書き換わる
- 検索してカーソルを合わせ、手で選択して貼り付けるほうが安全
- コミットメッセージの自動生成は的外れなことがあるが、中身には影響しない

### 環境

- 主な作業環境：iPhone の GitHub Web editor
- コマンドラインは使用しない
- 会社PCはセキュリティ制限で一部Googleサービスがブロックされる
