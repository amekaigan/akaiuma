# CLAUDE.md

このリポジトリで作業するすべてのエージェントは、本ファイルの内容を最優先で守ること。
判断に迷った場合は独自に解釈せず、必ずディレクター（親セッション）に確認を戻す。

関連ファイル。内容が食い違う場合は実装（`.github/scripts/generate-site.js`）を正とし、
ディレクターに報告すること。

| ファイル | 内容 |
|---|---|
| `CLAUDE.md` | エージェントが守るルール（このファイル） |
| `akaiuma-spec.md` | サイトの仕様書。安定した仕様はこちら |
| `akaiuma-handoff-vN.md` | 現在の作業状況・未完了の作業 |

---

## 0. サイト情報と表記ルール

```
ドメイン：akaiuma.com
屋号：あかいうま
タグライン：物販の売上アップと業務効率化
由来：赤兎馬 — 乗り手の力を引き出し、千里を走る馬
```

### 表記の使い分け（ぶらしてはならない）

| 場面 | 表記 |
|---|---|
| ロゴ・サイトタイトル・記事内の基本 | あかいうま |
| SNS表示名 | あかいうま｜物販の売上アップと業務効率化 |
| 文中で強調する場合のみ | 赤い馬 |
| 由来を語る場面のみ | 赤兎馬 |

読みは常に「あかいうま」。エージェントは原則 `あかいうま` を使い、
他の表記を使う場合はディレクターの指示があるときのみとする。

### ブランドの構造（記事のトーンに反映させる）

赤兎馬は乗り手を選ばず、乗った者の力を引き出した馬である。
**読者が乗り手であり、このサイトは馬である。主役は常に読者であって運営者ではない。**
「私がすごい」ではなく「あなたが速く走れる」という書き方を全記事で貫くこと。

---

## 1. プロジェクトの目的

物販で稼ぎたい個人・小規模事業者に向けた、実務ノウハウメディアを運営する。

収益は三層で考える。

| 層 | 内容 | 時期 | 位置づけ |
|---|---|---|---|
| 一次 | ASP経由のアフィリエイト報酬 | 3〜6ヶ月 | 初期キャッシュ |
| 二次 | 自社の在庫・需要予測ツールの月額課金 | 12ヶ月〜 | 本命。ストック型 |
| 三次 | 有料コンテンツ・個別支援 | 未定 | 需要が見えてから判断 |

**このメディアは「稼ぐ場所」ではなく「信頼を作り、ツールへ送る場所」である。**
短期の報酬のために記事の正確性を曲げてはならない。読者からの信頼のほうが事業価値が高い。
この優先順位を全エージェントが共有すること。

---

## 2. 読者像

物販で稼ぎたい、または稼ぎ始めたが伸び悩んでいる個人・小規模事業者。

### 二層を同時に扱う

| 層 | 状態 | 記事での役割 |
|---|---|---|
| 初心者 | 月商0〜30万。何を売るかで迷っている | 検索流入の入口。母数が最大 |
| 中級 | 月商50〜500万。伸び悩み、管理が破綻し始めている | 収益の主戦場。競合が最も薄い |

**開始時点では層を決め打ちしない。**両方に向けて記事を出し、反応を見て寄せる。
判断は3ヶ月後、数字にもとづいて行う（第7節）。

### 中級層が本命である理由

初心者向け市場は飽和しているが、「月商100万から先に進めない」層に向けて
具体的な話ができる書き手はほとんどいない。そして在庫・数字・複数チャネルで
悩み始めるのがこの層であり、自社ツールの購買層と完全に重なる。

### 初心者記事の位置づけ

初心者記事の目的はPVではなく**入口**である。
すべての初心者記事の末尾に、対応する中級記事への導線を必ず置くこと。
これを欠いた初心者記事は、母数を集めるだけで収益に寄与しない。

---

## 3. 運営者と開示のルール

### 名乗り方

- 屋号で運営する。実名・顔出しはしない
- 経歴と実績は記事内で明示してよい

### 実績の書き方（重要）

匿名で数字だけを掲げると「証明できない実績」になり、かえって信頼を損なう。

**実績は名乗るものではなく、記事の細部からにじませるものとする。**

- 悪い例：「月商1億円のECサイトを運営してきました」だけで終わる
- 良い例：複数チャネルの在庫を一元化しようとして最初に破綻した箇所を、具体的に書く

数字を書く場合は、必ずその数字を扱った経験がなければ書けない具体性を添えること。

### 開示義務

- アフィリエイトリンクを含む記事は、ページ上部にPR表記を置く
- 自社ツールに言及する記事は、運営者が開発・販売者である旨を明示する
- 「第三者の中立的な比較」を装う表現は使わない

### 有料販売時の注意（エージェントは判断せずディレクターに戻すこと）

有料のもの（ツールの月額課金、有料コンテンツ）を販売する場合、
特定商取引法にもとづく表示が必要になる。表示は法人名義で行う。
販売に関わるページを新規作成する場合は、必ずディレクターの確認を経ること。

---

## 4. 記事の品質基準

### 絶対に守ること

**この節はエージェントの執筆に対する制約である。運営者本人が加筆する部分には適用しない。**
運営者は20年の実務経験を持つ書き手であり、その経験にもとづく判断は
データで証明できなくても価値がある。エージェントがそれを真似てはならない、という趣旨。

1. **エージェントは、一次体験のない断定を書かない**
   実際に使っていないツールやサービスについて「使いやすい」「安定している」と
   書いてはならない。未検証の事項は「未検証」と明記するか、公式情報である旨を示す。
   ただし運営者が自分の経験から書く場合は、この限りではない。

2. **一般論で埋めない**
   「自分に合った方法を選びましょう」のような、誰でも書ける文章は削除対象。
   一文ごとに「これは実務経験のない書き手に書けるか？」を自問すること。
   書けるなら、その一文に価値はない。

3. **出典を持つ**
   手数料、規約、各モールの仕様は必ず公式情報を出典とする。
   まとめサイトや他人の記事を出典にしてはならない。

4. **情報の鮮度を記録する**
   手数料率、規約、ツールの料金には必ず確認日を添える。この分野は変更が頻繁。

5. **収益を保証しない**
   「必ず稼げる」「誰でも月◯万」といった表現を禁止する。
   再現性には条件があることを常に明示する。

6. **確度を書き分ける**
   記事に入る情報には三種類ある。公式情報にもとづく事実、実際に試して確かめたこと、
   経験からの推測。**どれを書いてもよいが、どれなのかが読者に分かるようにする。**
   禁止するのは断定そのものではなく、確度の異なるものが混ざったまま
   同じ強さで語られることである。

### 差別化の源泉

本メディアの唯一の武器は、運営者が現在も現場で手を動かしている点である。
以下を積極的に書くこと。

- 複数チャネル（楽天・Yahoo・Amazon・自社サイト）を同時運用して初めて表面化する問題
- 規模が拡大したときに壊れる運用と、その前兆
- 公式ドキュメントに書かれていない実際の挙動
- 失敗して損失を出した判断と、その原因

**AIは取材・構成・整形・校正を高速化する道具であり、専門性そのものを生成できない。**
実体験の注入は必ず人間（運営者）が行う。エージェントは注入箇所を空欄として明示し、
決して埋めてはならない。書式は以下で統一する。

```
<!-- TODO:実体験 何を書くべきか：（具体的な問い） -->
```

---

## 5. カテゴリとメタ情報

### カテゴリ

```
リサーチ | 仕入れ | 販路・集客 | 在庫管理 | 数字管理 | 業務効率化
```

カテゴリIDは順に `research` `sourcing` `sales` `inventory` `numbers` `efficiency`。
IDは記事一覧のアンカーに使う。表示名は `generate-site.js` の `CATEGORY_TO_ID` と
一字一句一致させること。一致しないとビルド時に「未定義のカテゴリ」の警告が出て、
その記事は一覧に載らない。

カテゴリはURL構造に影響する。追加・変更・統合を行う場合は、
リダイレクトの要否をディレクターが判断すること。エージェントが独断で変更してはならない。

### メタ情報（必須）

記事は `blog/<slug>/index.html` に HTML で書く。メタ情報はファイル冒頭の
HTMLコメントに記述する。**YAMLフロントマターは使わない。**

```html
<!--
title: 記事タイトル｜あかいうま
meta: 説明文（70文字以内）
permalink: /blog/slug/
published: YYYY-MM-DD
updated:
verified: YYYY-MM-DD
category: 在庫管理
level: beginner | intermediate
disclosure: affiliate | owned | both
thumb:
-->
```

- `level` は層別の反応を集計するための最重要フィールドである。省略を認めない
  （省略するとビルド時に警告が出る）
- `verified` は料金・仕様を確認した日。記事上部に表示される
- `disclosure` を入れると開示ブロックが自動挿入される。値は
  `affiliate`（アフィリエイトリンクあり）／`owned`（自社ツールに言及）／`both`
- `permalink` は `/blog/<slug>/` の形式。ディレクトリ名と一致させる

ヘッダー・目次・PR枠・関連記事・フッターは `generate-site.js` が
AUTOマーカーの中身を自動生成する。マーカーの一覧と本文で使うクラス
（`ak-article` `ak-note` `ak-source` `ak-table-wrap` `num`）は
`akaiuma-spec.md` の4章を参照。**マーカーの中身を手で書かない。**

---

## 6. SEOの方針

- 狙うキーワードは記事1本につき主1・副3まで
- タイトルは32文字以内。主キーワードは先頭30文字以内に置く（スマホ表示の制限）
- 見出しは検索意図の分解に対応させる。装飾的な見出しを作らない
- メタディスクリプションは70文字以内。具体的な名詞と数字を入れる
- アンカーテキストは内容を示すものにする。「こちらの記事」等は禁止
- 検索ボリュームが不明な場合は断定せず「要確認」と記す
- 内部リンクは公開時に3本以上。既存記事から新記事へも張り直す

**量産で勝とうとしない。**この分野は専業アフィリエイターと企業メディアが
長年張り付いている。記事数では勝てない。勝てるのは一次情報の深さだけである。

公開ペースは週4〜5本を上限とし、余力は既存記事の質の改善に回す。

---

## 7. 反応の測定と方針転換

開始から3ヶ月後、以下の指標を `level` 別に集計し、注力する層を決定する。

### 見る指標

- 滞在時間・読了率
- メール登録率
- アフィリエイトのクリック率と成約率
- SNSでの反応の質（「役に立った」か「もっと詳しく」か）

### 見てはいけない指標

**PV数だけで判断してはならない。**初心者向け記事は母数の差により必ずPVが多く出る。
これを根拠に初心者へ寄せると、単価の低い層に最適化されてしまう。

### 記事配分（最初の20本）

- 初心者向け 8本（検索流入の入口）
- 中級向け 8本（収益の主戦場）
- 両方に効くもの 4本（数字管理・在庫・複数チャネル）

---

## 8. ディレクトリ構成

```
CLAUDE.md                         このファイル
akaiuma-spec.md                   サイトの仕様書
akaiuma-handoff-v1.md             現在の作業状況
CNAME                             akaiuma.com
index.html                        トップページ
.claude/agents/                   各担当エージェントの定義（5体）
.github/scripts/generate-site.js  サイト生成スクリプト
assets/css/article.css            記事のCSS
assets/logo/                      ロゴ一式
blog/index.html                   記事一覧
blog/_template/index.html         記事テンプレート
blog/<slug>/index.html            個別記事
about/index.html                  このサイトについて
knowledge/                        運営者の一次情報（第9節）
```

記事は `blog/<slug>/index.html` に HTML で書く。記事URLは `/blog/<slug>/`。
新規記事は `blog/_template/index.html` を複製して作る。
`_` で始まるディレクトリは記事として扱われないため、テンプレートは一覧に出ない。

### まだ存在しないディレクトリ

以下はエージェントの出力先だが、リポジトリにはまだ存在しない。
最初に使う担当が作成する。

| ディレクトリ | 用途 | 担当 |
|---|---|---|
| `research/` | 調べた一次情報・出典メモ（`research/YYYY-MM-DD-テーマ.md`） | リサーチ |
| `keywords/` | キーワード設計表（`keywords/カテゴリ-slug.md`） | SEO |
| `sns/` | SNS投稿文のストック（`sns/YYYY-MM-DD.md`） | SNS |

---

## 9. knowledge/ — 運営者の一次情報

運営者が現場で得た素材を貯める場所。主力記事はここを素材に作る。

**`research/`（エージェントが調べた結果）とは別物である。混ぜてはならない。**

### タグ

素材には確度のタグが付く。記事に起こすときの扱いは以下で固定する。

| タグ | 意味 | 記事での扱い |
|---|---|---|
| `[実測]` | 自分でやって確かめた。数字や日付がある | 断定してよい |
| `[経験]` | 何度も経験して身についた判断 | 「経験上」として書く |
| `[推測]` | そう感じるが確証はない | 「〜と考えている」として書く |
| `[公式]` | 公式情報。確認日を添える | 出典付きで断定 |

第4節の「確度を書き分ける」はこのタグと対応している。
タグを無視して、確度の異なる素材を同じ強さで書いてはならない。
タグのない素材の確度は、エージェントが推測で決めずディレクターに確認を戻す。

### inbox.md

`knowledge/inbox.md` は運営者が思いついたことを放り込む場所。
iOSショートカット「あかいうま」から GitHub API 経由で追記され、
`## (YYYY-MM-DD HH:mm)` の見出しで区切られる。

- 整理・分類はしない運用。タグ付けと分類は後から行う
- **エージェントは読んでよいが、書き換えてはならない。**
  ショートカットは全文を取得して書き戻す仕組みのため、
  横から編集すると `sha` が食い違い、運営者の投稿が弾かれる
- 素材として使う場合は、該当箇所をディレクターに示して確認を取る

ショートカットの構成・GitHub API の詰まりやすい点は
`akaiuma-spec.md` の8章を参照。

---

## 10. エージェントの運用ルール

### 工程

```
ディレクター（企画・指示・最終判断）
  ├─ リサーチ担当    → research/ に出典付きで蓄積
  ├─ 記事作成担当    → blog/<slug>/index.html に草稿を書く
  ├─ SEO担当         → keywords/ にキーワード設計。公開前の最適化
  ├─ 校正担当        → 事実確認・表記統一
  └─ SNS担当         → sns/ に投稿文を出力
```

素材は二系統ある。運営者の経験が核になる主力記事は `knowledge/` を、
公式情報から書ける周辺記事は `research/` を素材にする。

サブエージェント同士は直接会話できない。報告は必ずディレクターに戻る。
ディレクターが受け取り、次の担当へ渡すハブ型で運用する。

### モデルの使い分け

使用量の枠は全チャネル共有で消費される。以下を基準とする。

| 担当 | モデル |
|---|---|
| ディレクター | Opus |
| 記事作成 | Sonnet（不足なら都度上げる） |
| リサーチ | Sonnet |
| SEO | Sonnet |
| 校正 | Sonnet |
| SNS | Haiku |

1セッションに全工程を詰め込まない。**工程ごとにセッションを切る。**
会話履歴の蓄積が消費の主因であり、これが最も効く節約になる。

---

## 11. 公開前チェックリスト

ディレクターが公開前に全項目を確認する。一つでも欠ける場合は公開しない。

- [ ] `level` と `category` が冒頭のメタコメントに入っているか
- [ ] `category` の表記が `CATEGORY_TO_ID` の表示名と一致しているか
- [ ] `permalink` がディレクトリ名と一致しているか
- [ ] `verified` が入っているか（料金・仕様に言及する場合）
- [ ] PR表記があるか（アフィリエイトリンクを含む場合。`disclosure` を入れたか）
- [ ] 自社ツールへの言及がある場合、運営者との関係を開示しているか
- [ ] `TODO:実体験` がすべて埋まっているか
- [ ] エージェントが書いた部分に、一次体験のない断定が残っていないか
- [ ] 収益を保証する表現がないか
- [ ] 出典が公式情報になっているか
- [ ] 初心者記事の場合、中級記事への導線があるか（自動挿入されるが確認）
- [ ] 内部リンクが3本以上あるか
- [ ] タイトルが32文字以内で、主キーワードが先頭30文字以内にあるか

---

## 12. 禁止事項

- 事実確認していない内容の断定
- 「必ず稼げる」「誰でも月◯万」等、収益を保証する表現
- 他社サービスの不当な貶め
- 運営者と自社ツールの関係の秘匿
- 検索順位のみを目的とした無内容な記事の量産
- 読者が実際に負えるリスクを無視した推奨（借入による仕入れ等）
- 記事の削除・大幅な書き換えを、ディレクターの確認なしに行うこと
- `knowledge/inbox.md` をエージェントが書き換えること
- AUTOマーカーの中身を手で書くこと（`generate-site.js` の生成物を壊す）
