# CLAUDE.md

このリポジトリで作業するすべてのエージェントは、本ファイルの内容を最優先で守ること。
判断に迷った場合は独自に解釈せず、必ずディレクター（親セッション）に確認を戻す。

---

## 0. サイト情報と表記ルール

```
ドメイン：akaiuma.com
屋号：あかいうま
タグライン：物販の売上アップと業務効率化
由来：赤兎馬 — 乗り手の力を引き出し、千里を走る馬
```

### 表記の使い分け（ぶらしてはならない）

| 場面 | 表記 |
|---|---|
| ロゴ・サイトタイトル・記事内の基本 | あかいうま |
| SNS表示名 | あかいうま｜物販の売上アップと業務効率化 |
| 文中で強調する場合のみ | 赤い馬 |
| 由来を語る場面のみ | 赤兎馬 |

読みは常に「あかいうま」。エージェントは原則 `あかいうま` を使い、
他の表記を使う場合はディレクターの指示があるときのみとする。

### ブランドの構造（記事のトーンに反映させる）

赤兎馬は乗り手を選ばず、乗った者の力を引き出した馬である。
**読者が乗り手であり、このサイトは馬である。主役は常に読者であって運営者ではない。**
「私がすごい」ではなく「あなたが速く走れる」という書き方を全記事で貫くこと。

---

## 1. プロジェクトの目的

物販で稼ぎたい個人・小規模事業者に向けた、実務ノウハウメディアを運営する。

収益は三層で考える。

| 層 | 内容 | 時期 | 位置づけ |
|---|---|---|---|
| 一次 | ASP経由のアフィリエイト報酬 | 3〜6ヶ月 | 初期キャッシュ |
| 二次 | 自社の在庫・需要予測ツールの月額課金 | 12ヶ月〜 | 本命。ストック型 |
| 三次 | 有料コンテンツ・個別支援 | 未定 | 需要が見えてから判断 |

**このメディアは「稼ぐ場所」ではなく「信頼を作り、ツールへ送る場所」である。**
短期の報酬のために記事の正確性を曲げてはならない。読者からの信頼のほうが事業価値が高い。
この優先順位を全エージェントが共有すること。

---

## 2. 読者像

物販で稼ぎたい、または稼ぎ始めたが伸び悩んでいる個人・小規模事業者。

### 二層を同時に扱う

| 層 | 状態 | 記事での役割 |
|---|---|---|
| 初心者 | 月商0〜30万。何を売るかで迷っている | 検索流入の入口。母数が最大 |
| 中級 | 月商50〜500万。伸び悩み、管理が破綻し始めている | 収益の主戦場。競合が最も薄い |

**開始時点では層を決め打ちしない。**両方に向けて記事を出し、反応を見て寄せる。
判断は3ヶ月後、数字にもとづいて行う（第7節）。

### 中級層が本命である理由

初心者向け市場は飽和しているが、「月商100万から先に進めない」層に向けて
具体的な話ができる書き手はほとんどいない。そして在庫・数字・複数チャネルで
悩み始めるのがこの層であり、自社ツールの購買層と完全に重なる。

### 初心者記事の位置づけ

初心者記事の目的はPVではなく**入口**である。
すべての初心者記事の末尾に、対応する中級記事への導線を必ず置くこと。
これを欠いた初心者記事は、母数を集めるだけで収益に寄与しない。

---

## 3. 運営者と開示のルール

### 名乗り方

- 屋号で運営する。実名・顔出しはしない
- 経歴と実績は記事内で明示してよい

### 実績の書き方（重要）

匿名で数字だけを掲げると「証明できない実績」になり、かえって信頼を損なう。

**実績は名乗るものではなく、記事の細部からにじませるものとする。**

- 悪い例：「月商1億円のECサイトを運営してきました」だけで終わる
- 良い例：複数チャネルの在庫を一元化しようとして最初に破綻した箇所を、具体的に書く

数字を書く場合は、必ずその数字を扱った経験がなければ書けない具体性を添えること。

### 開示義務

- アフィリエイトリンクを含む記事は、ページ上部にPR表記を置く
- 自社ツールに言及する記事は、運営者が開発・販売者である旨を明示する
- 「第三者の中立的な比較」を装う表現は使わない

### 有料販売時の注意（エージェントは判断せずディレクターに戻すこと）

有料のもの（ツールの月額課金、有料コンテンツ）を販売する場合、
特定商取引法にもとづく表示が必要になる。表示は法人名義で行う。
販売に関わるページを新規作成する場合は、必ずディレクターの確認を経ること。

---

## 4. 記事の品質基準

### 絶対に守ること

**この節はエージェントの執筆に対する制約である。運営者本人が加筆する部分には適用しない。**
運営者は20年の実務経験を持つ書き手であり、その経験にもとづく判断は
データで証明できなくても価値がある。エージェントがそれを真似てはならない、という趣旨。

1. **エージェントは、一次体験のない断定を書かない**
   実際に使っていないツールやサービスについて「使いやすい」「安定している」と
   書いてはならない。未検証の事項は「未検証」と明記するか、公式情報である旨を示す。
   ただし運営者が自分の経験から書く場合は、この限りではない。

2. **一般論で埋めない**
   「自分に合った方法を選びましょう」のような、誰でも書ける文章は削除対象。
   一文ごとに「これは実務経験のない書き手に書けるか？」を自問すること。
   書けるなら、その一文に価値はない。

3. **出典を持つ**
   手数料、規約、各モールの仕様は必ず公式情報を出典とする。
   まとめサイトや他人の記事を出典にしてはならない。

   **「〜は誤り」「〜にしか書かれていない」と断定する前に、一次資料の本文を開いて読む。**
   検索結果の抜粋や記憶だけで否定してはならない。否定は肯定より強い主張であり、
   外したときの損失も大きい。

   書き始める前に、**既存記事に同じ数値や論点の整理がないかを grep で確認する。**
   あれば、そちらへリンクするか、記述を一本化する。同じ話が二箇所にあると、
   片方を直したときにもう片方が古くなる。

4. **情報の鮮度を記録する**
   手数料率、規約、ツールの料金には必ず確認日を添える。この分野は変更が頻繁。

5. **収益を保証しない**
   「必ず稼げる」「誰でも月◯万」といった表現を禁止する。
   再現性には条件があることを常に明示する。

6. **確度を書き分ける**
   記事に入る情報には三種類ある。公式情報にもとづく事実、実際に試して確かめたこと、
   経験からの推測。**どれを書いてもよいが、どれなのかが読者に分かるようにする。**
   禁止するのは断定そのものではなく、確度の異なるものが混ざったまま
   同じ強さで語られることである。

### 読みやすさとリズム（全記事共通）

内容が正しくても、読み進める力がなければ届かない。
**基準は「声に出して読んで、つっかえないか」である。字数や文末の回数で機械的に整えない。**
（2026-09-25 の見直し：字数で文を切るルールが、文と文のつながりを消していた。
一文の平均は33字、20字以下の文が18%。自然に読める asiatopro は53字・9%だった）
- 一文で言うべきことは一文で言う。字数だけを理由に文を切らない
- 切るときは「だから」「ただし」「そのため」などで、前の文との関係を残す
- 読点が4つ以上続いて意味が追えない文だけ、切るか組み替える
- です・ます調で通す。リズムのために体言止めや「〜だ」「〜る。」を混ぜない
- 「この」「その」で始まる文が続いたら、何を指すかを書く
- 見出しの前に、決まった型の問いかけ（「では、〜のか。」）を置かない
- 「〜することができます」「〜という形になっています」等の冗長表現は短い形に直す
- 1段落で言うことは1つ。話題が変わったら改段する
- 文章だけが長く続く箇所には、視覚的な息継ぎ（装飾パーツ）を入れる

装飾は `assets/css/article.css` の「記事内の装飾パーツ」にあるクラスだけを使う。
記事側に `style` 属性や独自クラスを書いてはならない。

| クラス | 用途 |
|---|---|
| `p.ak-lead` | h2直後の一行要約 |
| `div.ak-point` | 要点・結論のまとめ |
| `p.ak-pull` | 核心の一文を抜き出す |
| `ol.ak-steps` | 順番に意味がある手順 |
| `ul.ak-check` | 今日やることのチェックリスト |
| `div.ak-compare` | 悪い例と良い例の対比 |
| `hr.ak-sep` | 場面転換の区切り（h2の直前には置かない） |
| `div.ak-note` | 運営者の実体験（ここだけは実体験専用） |
| `figure.ak-figure` | 図解（SVG）。長い記事に1〜2枚まで |

**入れすぎない。** h2ひとつにつきパーツは1つまで。パーツが多い記事はうるさくなる。
ただし `ak-note`（運営者の実体験）と `ak-figure`（図解）は、この数に含めない。
役割が装飾ではなく、記事の中身そのものだからである。

### 画像とサムネイル

- 記事中の画像は `assets/figure/` に置くSVGの図解とする。**記事の中身を図にしたものだけ**を置き、
  雰囲気だけの写真・イラストは使わない。素材写真はどの記事でも成立してしまい、一次情報の価値を薄める
- サムネイルは、画像がなければ生成スクリプトが**バナーを自動生成**する
  （`assets/thumb/auto/<slug>.svg`。手で触らない）。骨格は全記事で共通とし、
  変わるのはカテゴリの色・アイコンと、記事ごとの短いコピーだけ。
  テンプレートを1つに保つのは、記事ごとにデザインを考える工数を発生させないため
- コピーはメタ情報の `thumbcopy` に書く。**18字以内**。カードの下にはタイトルが並ぶので、
  タイトルの繰り返しにせず、記事の中身を一言で言い切る。省略した場合はタイトルから作る
- カテゴリの色とアイコンは `generate-site.js` の `CATEGORY_LOOK` にある。
  カテゴリを増やすときはここにも追加する（ディレクターが行う）
- 手で用意したい場合の優先順位：メタ情報の `thumb:` ＞ `assets/thumb/<slug>.(svg|png|webp|jpg)` ＞ 自動生成
- 図解をサムネイルに流用してもよいが、一覧では小さく表示されるため、
  細かい図は読めなくなる。原則は自動生成のバナーを使う
- SNSのOGP画像はSVGを表示できないため、`og:image` は従来どおりPNGを使う

読みやすさのための書き換えは、**言い回しに対して行うものであって、情報量を削ることではない。**
短くした結果、一次情報が落ちたなら、それは改悪である。

### 記事を開いた読者に、読む理由を渡す

タイトルの次がいきなり本文だと、読者が得るのは「長そう」という印象だけである。
以下は生成スクリプトが記事の冒頭に自動で入れる。書き手が用意するのは `takeaways` だけ。

| 要素 | 出どころ |
|---|---|
| サムネイルのバナー | 自動（一覧と同じ絵） |
| 読了時間（約◯分） | 自動（本文の文字数 ÷ 600字） |
| 読み進みのバー | 自動（画面上部） |
| この記事で分かること | メタ情報の `takeaways` |

`takeaways` は `|` 区切りで**3点**。記事の見出しの写しではなく、
**読者が読み終えたあとに何ができるようになるか**を書く。
「〜について解説」ではなく「〜を逆算する手順」のように、手に入るものを名詞で書き切る。

煽り（「知らないと損」等）は使わない。このメディアは信頼を作る場所であり、
冒頭で釣ると、本文の正確さごと安く見える。

### 差別化の源泉

本メディアの唯一の武器は、運営者が現在も現場で手を動かしている点である。
以下を積極的に書くこと。

- 複数チャネル（楽天・Yahoo・Amazon・自社サイト）を同時運用して初めて表面化する問題
- 規模が拡大したときに壊れる運用と、その前兆
- 公式ドキュメントに書かれていない実際の挙動
- 失敗して損失を出した判断と、その原因

**AIは取材・構成・整形・校正を高速化する道具であり、専門性そのものを生成できない。**
実体験の注入は必ず人間（運営者）が行う。エージェントは注入箇所を空欄として明示し、
決して埋めてはならない。書式は以下で統一する。

```
<!-- TODO:実体験 何を書くべきか：（具体的な問い） -->
```

### 数値が確認できないときは、空欄で確保する

手数料・料金・料率は、公式ページの原文を確認できたものだけを書く。
検索結果の要約や他社の比較記事から拾った数値を、確認済みのように書いてはならない。

確認できていない数値は、推測で埋めず、次の書式で空欄として残す。

```
<!-- TODO:数値 何を入れるか：（項目名）／出典：（公式URL）／確認日：（入れた日） -->
```

**空欄が残っている記事は公開しない。**ただし、空欄のまま本文の論旨が成立するなら、
その記事は数値がなくても価値があるということでもある。
数値を並べるだけの比較は競合が大量に書いている。勝てるのは、その数値を扱った経験のほうである。

---

## 5. カテゴリとメタ情報

### カテゴリ

```
リサーチ | 仕入れ | 販路・集客 | 在庫管理 | 数字管理 | 業務効率化
```

カテゴリはURL構造に影響する。追加・変更・統合を行う場合は、
リダイレクトの要否をディレクターが判断すること。エージェントが独断で変更してはならない。

### タグ

```
楽天市場 | Amazon | Yahoo!ショッピング | 自社サイト | 複数チャネル運用
```

カテゴリが「何の話か」の軸であるのに対し、タグは「どのチャネルの話か」の軸である。
同じ「売上アップ」でも楽天とAmazonではやり方が違うため、チャネルで横断して
記事を集められるようにしている。

使える名前は `.github/scripts/generate-site.js` の `TAG_TO_ID` にあるものだけ。
辞書にない名前を書くと生成時に警告が出て無視される（「楽天」「楽天市場」のような
表記ゆれでタグが分裂するのを防ぐため）。**タグの追加はディレクターが行う。**
エージェントが辞書を書き換えてはならない。

同じタグの記事が3本たまると `/tag/<id>/` が自動生成される。2本以下のタグは
記事下に表示されるだけでページを持たない。中身の薄いページを作らないためである。

### メタ情報（必須）

記事は `blog/<slug>/index.html` の**先頭にHTMLコメント**でメタ情報を書く。
この形式でないと `generate-site.js` が記事として認識せず、サイトに一切出ない。

```html
<!--
title: 記事タイトル｜あかいうま
meta: 検索結果に出る説明文。70文字以内
permalink: /blog/<slug>/
published: YYYY-MM-DD
updated:                             # 更新したときだけ書く
verified: YYYY-MM-DD                 # 料金・規約の確認日。記載がなければ空でよい
category: リサーチ | 仕入れ | 販路・集客 | 在庫管理 | 数字管理 | 業務効率化
level: beginner | intermediate       # 反応測定に使う。必須
tags: 楽天市場, Amazon                # 任意。カンマ区切り
disclosure: affiliate | owned | both  # 開示ブロックの出し分け。不要なら空
thumb:                               # 任意。手で用意した画像を使う場合のパス
thumbcopy:                           # 任意。サムネイルに載せる短いコピー。18字以内
takeaways:                           # 任意。この記事で分かること。| 区切りで3点
search_intent:                       # 検索者の問いを1文で
title_reason:                        # なぜこのタイトルがその問いへの答えなのか
-->
```

値がないフィールドは、キーだけ残して空にする。

**`verified` は、料金・料率・規約・各モールの仕様を本文に書いた記事にだけ入れる。**
これらの記載が一つもない記事では空のままでよい。

この項目は情報の鮮度を保証するためのものであって、書式を揃えるためのものではない。
確認していない日付を埋めると、鮮度の保証そのものが嘘になる。
**空欄は正直さであり、不備ではない。**

`search_intent` と `title_reason` は、記事の切り口が検索意図から外れていないかを
後から検証するために残す。生成スクリプトはこの2つをページに出力しない。
**下書きの報告とコミットメッセージには、この2行と「差別化をどう出したか」を書く。**
運営者はそこだけを見て、切り口が合っているかを判断できる。

`level` は層別の反応を集計するための最重要フィールドである。省略を認めない。

---

## 6. SEOの方針

### 記事の切り口を決める（執筆より前に必ず行う）

構成を書き始める前に、この5つを通す。順番を飛ばさない。

1. **検索者の問いを1文で書く。** 読者が検索窓に何を入れ、何を知りたいのか
2. **タイトルは、その問いへの答えにする。** 意外な事実があるなら前半に置く
3. **差別化は「書き方」で出し、「切り口」にしない。**
   本メディアの武器は一次情報の深さであって、変わった角度ではない。
   奇をてらった切り口は、検索意図から外れて誰にも届かない
4. **調査中に見つけた食い違いや発見は、本文の1節に置く。**
   タイトルやリードの軸にしない（運営者が求めた場合を除く）。
   発見は記事の価値を上げるが、それ自体が読者の探していたものとは限らない
5. **運営者が理由や仮説を示している場合、それを前提にする。**
   別の仮説を立てるなら、執筆前にその旨を明示して確認する。
   運営者の経験にもとづく判断を、エージェントの推測で上書きしてはならない



- 狙うキーワードは記事1本につき主1・副3まで
- タイトルは32文字以内。主キーワードは先頭30文字以内に置く（スマホ表示の制限）
- 見出しは検索意図の分解に対応させる。装飾的な見出しを作らない
- メタディスクリプションは70文字以内。具体的な名詞と数字を入れる
- アンカーテキストは内容を示すものにする。「こちらの記事」等は禁止
- 記事のタイトルを文の中にそのまま貼らない。文の流れに合わせて、
  リンク先の中身を示す言葉にする（例：「どの販路から始めるかを書いた記事」）
- 検索ボリュームが不明な場合は断定せず「要確認」と記す
- 内部リンクは公開時に3本以上。既存記事から新記事へも張り直す

**量産で勝とうとしない。**この分野は専業アフィリエイターと企業メディアが
長年張り付いている。記事数では勝てない。勝てるのは一次情報の深さだけである。

公開ペースは週4〜5本を上限とし、余力は既存記事の質の改善に回す。

---

## 7. 反応の測定と方針転換

開始から3ヶ月後、以下の指標を `level` 別に集計し、注力する層を決定する。

### 見る指標

- 滞在時間・読了率
- メール登録率
- アフィリエイトのクリック率と成約率
- SNSでの反応の質（「役に立った」か「もっと詳しく」か）

### 見てはいけない指標

**PV数だけで判断してはならない。**初心者向け記事は母数の差により必ずPVが多く出る。
これを根拠に初心者へ寄せると、単価の低い層に最適化されてしまう。

### 記事配分（最初の20本）

- 初心者向け 8本（検索流入の入口）
- 中級向け 8本（収益の主戦場）
- 両方に効くもの 4本（数字管理・在庫・複数チャネル）

---

## 8. ディレクトリ構成

```
CLAUDE.md              このファイル（運用ルールの正本）
README.md              リポジトリの案内図（人間向け）
.claude/agents/        各担当エージェントの定義
.claude/workflows/     Routine（自動実行）の手順書
.github/scripts/       サイト生成スクリプト（generate-site.js）
.github/workflows/     GitHub Actions
.github/spec/          仕様書（akaiuma-spec.md）
.github/handoff/       引き継ぎ資料。古い版は archive/ に移す
blog/                  記事本文（HTML）。1記事につき1ディレクトリ
blog/_template/        新規記事のひな形。これを複製して書く
tag/                   タグページ（自動生成。手で触らない）
assets/                CSS・ロゴ・図解・サムネイル
knowledge/             運営者の一次情報（体験バンク・質問キュー・inbox 等）
research/              収集した一次情報・出典メモ
keywords/              キーワード設計表
sns/                   SNS投稿文のストック（日付別。まだ無い）
scripts/               YouTube台本（まだ無い）
```

リポジトリの直下には、サイトとして公開するファイルと `CLAUDE.md`・`README.md` だけを置く。
仕様書・引き継ぎ資料などの作業用の文書は `.github/` の下に置く。

記事ファイルは `blog/<slug>/index.html` とする。slug は英小文字とハイフン。
日付はファイル名ではなく、メタ情報の `published` で持つ。

### 生成スクリプトの実行

記事を追加・修正したあとは、必ず以下を実行する。

```
node .github/scripts/generate-site.js
```

ヘッダー・フッター・記事一覧・トップ・タグページ・sitemap は、すべてこの
スクリプトが生成している。**実行しなければ、記事を書いてもサイトには出ない。**

main に push したときは GitHub Actions（`.github/workflows/build-site.yml`）が
同じスクリプトを実行し、生成結果を自動でコミットする。ただし公開前の確認は
手元で実行して行うこと。Actions は実行漏れの保険であって、確認の代わりではない。

---

## 9. エージェントの運用ルール

### 工程

```
ディレクター（企画・指示・最終判断）
  ├─ リサーチ担当    → research/ に出典付きで蓄積
  ├─ 記事作成担当    → blog/<slug>/index.html に草稿を書く
  ├─ エディター      → 読みやすさ・リズム・装飾を整える（内容には触らない）
  ├─ SEO担当         → キーワード設計と公開前の最適化
  ├─ 校正担当        → 事実確認・表記統一
  └─ SNS担当         → sns/ に投稿文を出力
```

サブエージェント同士は直接会話できない。報告は必ずディレクターに戻る。
ディレクターが受け取り、次の担当へ渡すハブ型で運用する。

### モデルの使い分け

使用量の枠は全チャネル共有で消費される。以下を基準とする。

| 担当 | モデル |
|---|---|
| ディレクター | Opus |
| 記事作成 | Sonnet（不足なら都度上げる） |
| エディター | Sonnet |
| リサーチ | Sonnet |
| SEO | Sonnet |
| 校正 | Sonnet |
| SNS | Haiku |

1セッションに全工程を詰め込まない。**工程ごとにセッションを切る。**
会話履歴の蓄積が消費の主因であり、これが最も効く節約になる。

---

## 10. 公開前チェックリスト

ディレクターが公開前に全項目を確認する。一つでも欠ける場合は公開しない。

- [ ] `level` と `category` がメタ情報に入っているか
- [ ] `verified` が入っているか（※料金・料率・規約・仕様の記載がある記事のみ。
      記載がない記事は空でよい。確認していない日付で埋めてはならない）
- [ ] 生成スクリプトを実行し、一覧とトップに記事が出ているか
- [ ] PR表記があるか（アフィリエイトリンクを含む場合）
- [ ] 自社ツールへの言及がある場合、運営者との関係を開示しているか
- [ ] `TODO:数値` がすべて埋まっているか
- [ ] `TODO:実体験` がすべて埋まっているか
      （※運営者が本文を読んで公開OKを出した場合は、空欄が残っていても公開してよい。
      空欄の直前に、答えを前提にした文を残さない。答えが届いたら追記して更新する）
- [ ] エージェントが書いた部分に、一次体験のない断定が残っていないか
- [ ] 収益を保証する表現がないか
- [ ] 出典が公式情報になっているか
- [ ] 初心者記事の場合、中級記事への導線があるか
- [ ] 内部リンクが3本以上あるか（※記事5本目以降に適用する。それ以前は
      張れるだけ張り、記事が増えた時点で既存記事から張り直す。
      立ち上げ期に記事の公開を止めないための例外であり、
      5本目からは本則どおり運用する）
- [ ] タイトルが32文字以内で、主キーワードが先頭30文字以内にあるか
- [ ] `search_intent` と `title_reason` が書かれ、タイトルが検索者の問いへの答えになっているか
- [ ] 調査中の発見を、タイトルやリードの軸にしていないか（本文の1節に置いているか）
- [ ] エディターを通し、一文の長さ・段落の重さ・視覚的な息継ぎが整っているか
- [ ] 装飾が `article.css` のパーツだけで組まれているか（`style` 属性の直書きがないか）

---

## 11. 禁止事項

- 事実確認していない内容の断定
- 「必ず稼げる」「誰でも月◯万」等、収益を保証する表現
- 他社サービスの不当な貶め
- 運営者と自社ツールの関係の秘匿
- 検索順位のみを目的とした無内容な記事の量産
- 読者が実際に負えるリスクを無視した推奨（借入による仕入れ等）
- 記事の削除・大幅な書き換えを、ディレクターの確認なしに行うこと

---

## 12. 本番への反映と、運営者への報告

### 本番への反映

本番（main）への反映は、原則として運営者に聞かずに行ってよい。
`.claude/settings.json` で許可済みである。

ただし、**サイトの見た目や読者の目に触れるものを変えるときは、反映の前に運営者のOKを取る。**
対象は、記事（`blog/`）・トップ（`index.html`）・`about/`・`assets/` などである。

OKをもらったら、運営者に操作させず、エージェントが自分で反映する。

### 運営者への報告

運営者への報告では専門用語を避け、次の言い換えを使う。

| 専門用語 | 報告での言い方 |
|---|---|
| main | 本番 |
| ブランチ | 作業用のコピー |
| PR | 本番への反映の申請 |
| マージ | 本番に反映 |
| Routine | 定期実行 |
